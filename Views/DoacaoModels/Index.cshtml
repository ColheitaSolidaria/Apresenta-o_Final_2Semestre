@model IEnumerable<ColheitaSolidaria.Models.DoacaoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodRecebedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodDoador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataValidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodRecebedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodDoador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataValidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodDoacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodDoacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodDoacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
