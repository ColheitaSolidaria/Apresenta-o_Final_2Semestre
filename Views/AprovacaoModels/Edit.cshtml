@model ColheitaSolidaria.Models.AprovacaoModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AprovacaoModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CodDoacao" />
            <div class="form-group">
                <label asp-for="CodAdm" class="control-label"></label>
                <input asp-for="CodAdm" class="form-control" />
                <span asp-validation-for="CodAdm" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DataAprovacao" class="control-label"></label>
                <input asp-for="DataAprovacao" class="form-control" />
                <span asp-validation-for="DataAprovacao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
